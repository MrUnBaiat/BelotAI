[pytest]
# Only the project's own suite. Without this a bare `pytest` also collects
# whatever happens to live in ignored working directories (old backups, scratch
# experiments), which is noise at best and a spurious failure at worst.
testpaths = tests
