# Core. Any recent PyTorch works; the models are small and CPU is viable for
# everything except training and the larger evaluations.
torch>=2.0
numpy>=1.24

# Training curves.
tensorboard>=2.12

# Tests.
pytest>=7.0

# The belot.md SDK, used by the online layer (belot/online, scripts/play_online.py,
# tools/verify_online.py) and its tests. The `live` extra is the websockets client the
# SDK needs to reach belot.md. Installed straight from GitHub and pinned to
# a commit, so a change to the SDK cannot silently change this project; update the
# commit when you update the SDK. To work on both repositories at once, run
# `pip install -e ../BelotMDPlayer` after installing this file.
belotmd[live] @ git+https://github.com/MrUnBaiat/belotmd.git@be700440ec87aebc683773ef5968423b275183d1
